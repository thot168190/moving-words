# 코다리 지시서 — 2026-08-28 · 챕터 정리 인계

> 발행: 로부장 | 대상: 코다리
> **판정은 코다리가 하지 않는다.** 출력만 주면 로부장·대표님이 판정한다.
> 막히면 **멈추고 보고**한다. 추측으로 진행하지 않는다.

---

## 공통 금지 (변동 없음)

- 🚫 `npm run build` — 심사 중. 번들 해시가 바뀌면 검증 경로가 깨진다
- 🚫 `git commit` · `git push` · Netlify 배포
- 🚫 **파이썬 실행** (로컬 3.14 세그폴트). **node만 사용**
- 🚫 「완료했습니다」로 보고하기. **명령 출력 원문**만 인정
- 🚫 단어 추가·삭제·재배정 — 대표님 지시 없이 한 글자도
- 🚫 `public/learning/index.html` 을 로부장과 동시에 편집 — 손대기 전 `ls -la` 로 수정 시각을 먼저 확인한다

---

# A. 오늘 로부장이 앱에 한 변경 (사실 기록 · 이미 저장됨)

대상 파일: `public/learning/index.html`
백업: `public/learning/index.html.bak_0828_전수조사전`

## A-1. 기준 문서를 찾았다

`_작업/1200_레벨태그_v3.csv` (1,202행 · 열: `단어,갈래,레벨,CEFR근거`)

**이것이 레벨 기준 정본이다.**

- `CEFR근거` 가 **A1 · A2 → 레벨1**
- `CEFR근거` 가 **B1 · B2 → 레벨2**

챕터3 단어는 109개 전부 이 표 안에 있었고, 그중 **40개(37%)가 앱에서 반대 슬롯에 놓여 있었다.** 전부 바로잡았다.

## A-2. 챕터 이동

`chapters` 배열 기준 — 챕터3은 **"우리 집"(DOMUS)** 이다.

| 방향 | 편 | 사유 |
|---|---|---|
| 챕터3 → 챕터2 | 호랑나비 · 푸른박새 · 무당벌레 · 돌 모이그릇 · 달팽이 | 숲과 생명. 그림도 `ch2/` 원본 |
| 챕터1 → 챕터3 | 옷걸이의 옷 · 맑은 욕실 · 계단 옆 거실 · 램프 곁의 담요 | 집 안. 챕터1은 "세상을 발견하는 순간" |

새 번호는 **챕터2 = 2-19~2-23**, **챕터3 = 3-17~3-20** 으로 부여했다.

```
챕터1  14편 → 10편
챕터2  18편 → 23편
챕터3  21편 → 20편
```

⚠️ **폴더명으로 챕터를 판단하지 말 것.** `ch3/` 폴더에는 놀이공원(→챕터5)·부엌 조리(→챕터6) 그림이 섞여 있다. 파일 폴더는 생성 순서일 뿐 챕터 배정이 아니다.

## A-3. 단어 삭제 32개

그림을 16장 전부 눈으로 확인하고, **화면에서 손으로 짚을 수 없는 단어**만 뺐다.

예: `villa/별장`(그림은 시골집) · `empty/비어있는`(유리병에 다육식물이 가득) · `step/발판`(손수레에 발판 없음, 지지다리임) · `link/잇는 쇠`(그건 황동 나사다) · `sheet/시트`(거실 그림에 침대 시트 없음)

## A-4. 뺀 단어의 자리 추적 (대표님 지시)

> "1200단어 안에 있는 단어이면 어딘가에 자리가 있을거야"

| 갈래 | 개수 | 처리 |
|---|---|---|
| 이미 다른 편에 있었음 (중복이었음) | 8 | 삭제로 종결 |
| 그림에서 짚히는 자리로 이동 | 9 | 아래 표 |
| 아직 자리 없음 | 15 | **B-3 참조** |

이동한 9개 — 좌표는 로부장이 그림을 보고 지정했다.

| 단어 | 간 곳 | 짚는 대상 | 좌표 |
|---|---|---|---|
| `cable` | 3-20 램프 곁의 담요 | 바닥 전선 | `[72,74]` |
| `step` | 3-19 계단 옆 거실 | 계단 디딤판 | `[30,48]` |
| `empty` | 3-14 외발 손수레 | 텅 빈 통 | `[46,40]` |
| `single` | 3-16 해바라기 | 한 송이 | `[43,38]` |
| `extend` | 3-07 화분 속 새싹 | 뻗은 잎 | `[55,33]` |
| `bend` | 3-08 물뿌리개 | 휜 손잡이 | `[18,45]` |
| `dump` | 3-01 조용한 부엌 | 쓰레기통 | `[60,80]` |
| `mess` | 3-05 현관 잡동사니 | 어질러진 짐 | `[55,45]` |
| `bare` | 3-06 거울 앞 선반 | 맨 두상 | `[20,35]` |

---

# B. 코다리 작업 — 위에서부터 순서대로

## 🔴 작업 1 — 무결성 검증 (가장 먼저)

로부장이 만진 파일이 깨지지 않았는지 확인한다. **판정하지 말고 출력만 보고한다.**

`_작업/verify_0828.js` 로 저장하고 `node _작업/verify_0828.js` 실행:

```js
const fs = require('fs');
const s = fs.readFileSync('public/learning/index.html', 'utf8');
const st = s.indexOf('const chapterData = ');
const b = s.indexOf('{', st);
let d = 0, k;
for (k = b; k < s.length; k++) {
  if (s[k] === '{') d++;
  else if (s[k] === '}') { d--; if (d === 0) break; }
}
const D = JSON.parse(s.slice(b, k + 1));

let slots = 0, nulls = 0, mism = 0;
for (const cn of Object.keys(D).sort((a, z) => a - z)) {
  const ch = D[cn];
  const n = ch.works.length;
  for (const key of ['levelOneWords','levelTwoWords','levelOneSpots','sceneSpots']) {
    if (ch[key].length !== n) console.log(`❌ ch${cn} ${key} 길이 ${ch[key].length} != works ${n}`);
  }
  let t = 0;
  for (let i = 0; i < n; i++) {
    const w = ch.works[i];
    const a = ch.levelOneWords[i].length, c = ch.levelTwoWords[i].length;
    t += a + c; slots += a + c;
    if (a !== ch.levelOneSpots[i].length) { console.log(`❌ ${cn}-${w.n} L1 단어${a} 좌표${ch.levelOneSpots[i].length}`); mism++; }
    if (c !== ch.sceneSpots[i].length)    { console.log(`❌ ${cn}-${w.n} L2 단어${c} 좌표${ch.sceneSpots[i].length}`); mism++; }
    for (const sp of [...ch.levelOneSpots[i], ...ch.sceneSpots[i]])
      if (sp === null || !Array.isArray(sp) || sp.length !== 2) { console.log(`❌ ${cn}-${w.n} 좌표이상 ${JSON.stringify(sp)}`); nulls++; }
    const wl = (w.words || []).length;
    if (wl !== a + c) console.log(`❌ ${cn}-${w.n} words${wl} != L1+L2 ${a + c}`);
  }
  console.log(`ch${cn}  ${n}편  ${t}단어`);
}
console.log(`총 슬롯 ${slots} / 좌표이상 ${nulls} / 단어·좌표 불일치 ${mism}`);
```

**기대값** — ch1 10편 72 / ch2 23편 137 / ch3 20편 83.
**챕터4에서 좌표이상 50건이 나오는 것은 기존 문제다.** 작업 2에서 다룬다.

## 🔴 작업 2 — 챕터4 좌표 빈칸 50건

8/28 01:59 주입 때 생긴 것으로 보인다. 좌표가 `null` 이면 그림 위 물음표가 안 찍혀 화면이 깨진다.

**고치지 말고 먼저 조사만 한다.**

```bash
ls -la public/learning/index.html*
```

로 백업 목록을 뽑고, `index.html.bak_0827_1500` (좌표이상 0건이던 시점) 의 챕터4 좌표와 현재를 대조해 **어느 편의 어느 단어가 좌표를 잃었는지** 목록으로 보고한다. 복구할지는 대표님이 정한다.

## 🟡 작업 3 — 목록 UI 확장 (기존 이슈)

앱 카드가 **레벨1 4개 + 레벨2 4개 고정**인데, 실제 데이터는 편당 1~6개로 가변이다.

지금 챕터3에는 `3-07 화분 속 새싹`처럼 **한쪽이 0개인 편**이 있다. 0개일 때 빈 칸이나 오류가 나지 않고 자연스럽게 접히는지 확인하고, 안 되면 고친다.

**이건 코다리 몫이다.** 데이터는 건드리지 말고 렌더링만 고친다.

## 🟢 작업 4 — 대기 (착수 금지)

아직 자리를 못 찾은 단어 15개다. **아무 그림에나 넣지 말 것.** 지금까지 문제가 다 이렇게 생겼다.

```
villa  breathe  operate  remain  survive  mass  gain
constant  maintain  force  shift  raise  super  continue  sheet
```

로부장이 챕터2·챕터4의 빈자리 편(여우 발자국·잎벌레·깃털·단풍씨앗·새알·핸드벨·스포이트 등 12편) 그림을 확인한 뒤 배정안을 낸다. **그 전까지 착수 금지.**

---

# C. 보고 형식

각 작업마다 이렇게 준다.

```
[작업 N]
실행한 명령:
<명령 원문>

출력:
<출력 원문 그대로 — 요약하지 말 것>

막힌 곳: (있으면)
```

「완료했습니다」만 적힌 보고는 인정하지 않는다.
