# 코다리 지시서 — 프로젝트 파일 정리 v2 (2026-08-01)

> 성격: 기계 작업(판단·창작 없음). 이동만 하고 **삭제는 절대 금지**.
> 1차 정리(로부장, _작업 서랍화 + 98_루트정리보관)는 완료 상태. 이번엔 프로젝트 루트 마감.

## A. 이동 대상 (움직이는그림사전/ 루트 → 98_루트정리보관/)
- `venv/` (구형 파이썬 가상환경 — 사용 중단)
- `hf-space/` (실험 잔재)
- `_to_delete/` 내용물 전부 (이름과 달리 지우지 말고 98로)
- 루트에 남은 기타 실험 파일(*.py, *.png, *.pdf, 임시 *.md 중 아래 B 목록에 없는 것)

## B. 이동 금지 (건드리면 빌드·배포·공장이 깨진다)
`src/` `public/` `index.html` `package.json` `package-lock.json` `vite.config.ts` `tsconfig*.json`
`tailwind.config.js` `postcss.config.js` `firebase.json` `.firebaserc` `firestore.rules` `.env.example`
`verify_ch1.mjs` `.agents/` `.gitignore` `data/` `dist/` `dist-final/` `node_modules/` `.git/`
`_작업/` 전체 (내부 재배치도 금지 — 00_파일지도.md가 정본 구조)

## C. 마무리
1. `npm run build` 실행 — **성공해야 정리 완료** (정리로 빌드가 깨지지 않았음을 증명)
2. `git add -A → commit "chore: 프로젝트 루트 파일 정리 v2" → push origin main`
3. 대기 중인 로부장 소스 수정분이 미커밋 상태면 별도 커밋으로 먼저 처리 후 정리 커밋

## D. 보고 양식
이동한 파일 목록 원문(mv 로그) + 루트 `ls` 원문 + build 성공 로그 마지막 3줄 + 커밋 해시.
"완료했습니다" 한 줄 보고는 미완료 처리.

## 금지
파일 삭제 / B 목록 접촉 / _작업 내부 재배치 / 파일 내용 수정 (이동만)
