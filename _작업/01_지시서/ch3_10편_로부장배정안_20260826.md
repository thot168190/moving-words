# ch3 정원 10편 — 로부장 배정안 (2026-08-26)

포스터 10장을 직접 열어 확인하고 짠 것입니다. **그림에 실제로 보이는 것만** 넣었고,
애매한 추상어는 뺐습니다. 그래서 편당 개수가 3~7개로 다릅니다.

정본 1200 안에서만 골랐고, 다른 편과 겹치는 단어는 없습니다.

| 편 | 제목 | 레벨1 (퀴즈·그림에 보임) | 레벨2 (스토리 확장) |
|---|---|---|---|
| ch3_11 | 화분 속 새싹 | `shape` 모양새 · `single` 한 포기 · `extend` 뻗어나가다 · `contain` 담고 있다 | `alive` 살아 있는 · `breathe` 숨 쉬다 · `develop` 자라나다 |
| ch3_12 | 물뿌리개와 모종삽 | `tool` 연장 · `handle` 손잡이 · `dig` 파다 · `operate` 다루다 | — |
| ch3_13 | 유리 온실 병 | `triangle` 세모 · `volume` 부피 · `empty` 비어 있는 · `include` 함께 들어 있다 | `remain` 그대로 있다 |
| ch3_14 | 꽃가위와 라벤더 | `pair` 한 쌍 · `piece` 조각 · `divide` 가르다 · `snap` 싹둑 자르다 | `remove` 쳐내다 · `bend` 휘다 |
| ch3_15 | 그루터기 위 새집 | `frame` 뼈대 · `block` 토막 · `settle` 자리 잡다 | `survive` 견디어 내다 |
| ch3_16 | 바구니에 담은 가을 | `bunch` 다발 · `mass` 덩어리 · `plenty` 넉넉함 · `several` 여럿의 | `gain` 거두다 |
| ch3_17 | 꽃을 누르는 나무틀 | `square` 네모 · `link` 잇는 쇠 · `shut` 꽉 닫다 · `bind` 묶어 두다 | `constant` 변함없는 · `maintain` 그대로 지키다 |
| ch3_18 | 외발 손수레 | `wide` 넓은 · `step` 발판 · `roll` 구르다 · `drag` 끌다 | `force` 밀어붙이는 힘 · `shift` 옮기다 |
| ch3_19 | 유리로 지은 온실 | `grand` 웅장한 · `upper` 위쪽의 · `internal` 안쪽의 | `raise` 높이다 |
| ch3_20 | 울타리 앞 해바라기 | `regular` 고르게 늘어선 · `combine` 엇갈려 짜이다 · `gorgeous` 눈부신 · `super` 아주 큰 | `continue` 계속 자라다 |

## 함께 필요한 코드 수정 2줄
레벨1이 4개 미만이면 퀴즈 문구가 틀리게 나옵니다.

```
roundText.textContent='0 / 4'        →  roundText.textContent='0 / '+targets.length
`${work.title}의 네 단어를 모두...`   →  `${work.title}의 단어를 모두...`
```

## 주입 방법
`python3 _작업/01_지시서/ch3_10편_로부장배정안_20260826.py`

좌표는 단어에 붙여 두었으므로 순서가 어긋나지 않습니다.
검수 이미지: `_작업/_검수_ch3/00_전체.jpg`
