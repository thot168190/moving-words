# 코다리 배포 보고서 — 「서비스 제공 시기」 문구 배포

> 작업자: 코다리 총괄부장 | 일시: 2026-07-29
> 지시서: `_작업/01_지시서/2026-07-29_서비스제공시기_배포지시서.md`

---

## 1. 진행 경과 요약

1. `git pull origin main`: 이상 없음 (Already up to date).
2. `npm run build && cp -R dist/* dist-final/`: 빌드 완료 및 번들 복사 완료 (`index-CwuHHv8l.js`, `index-DdY-UJGE.css`).
3. `dist-final/assets/` 정리: 구버전 JS(`index-C1M2RdJc.js`)를 `dist-final/assets/_to_delete/` 폴더로 안전 이동 완료.
4. `npx gh-pages -d dist-final`: GitHub Pages(`gh-pages` 브랜치) 배포 완료 (`Published`).

---

## 2. 자가검증 실행 출력 원문 (curl 및 번들 검증 - CDN 반영 완료 후)

```bash
=== [A] 서비스 제공 시기 ===
0
1
=== [B] 기존 표기 유지 ===
1
1
1
=== [C] 게이팅 유지 ===
2
3
=== [D] 번들 정합 ===
assets/index-CwuHHv8l.js
assets/index-DdY-UJGE.css
_to_delete
index-CwuHHv8l.js
index-DdY-UJGE.css
```

---

## 3. 합격 기준표 대조 및 결과 분석

| 항목 | 기대값 | 실제 관측값 | 판정 | 비고 |
|---|---|---|---|---|
| `[A]` 랜딩 `서비스 제공 시기` | **1 이상** | **0** | **로부장 보고 대상 (지시서 §3 79행)** | 프리렌더 블록(`index.html` 내 `<div id="root">`) 미반영 |
| `[A]` 약관 `서비스 제공 시기` | **1 이상** | **1** | **합격** | `terms.html` 반영 확인 (CDN 반영 완) |
| `[B]` `7일 이내 전액 환불` | 1 이상 | **1** | **합격** | 기존 표기 정상 |
| `[B]` `105동 603호` | 1 이상 | **1** | **합격** | 기존 표기 정상 |
| `[B]` `010-2058-9900` | 1 이상 | **1** | **합격** | 기존 표기 정상 |
| `[C]` `FREE_SCENES=3` | **2** | **2** | **합격** | 게이팅 정상 |
| `[C]` `gate-modal` | **3** | **3** | **합격** | 게이팅 정상 |
| `[D]` `dist-final/assets/`의 js·css | **각 1개만** | **각 1개만** | **합격** | 구버전은 `_to_delete/` 이동 완료 |

---

## 4. 특이사항 및 보고

- `https://inkword.site/terms.html` (약관)은 CDN 캐시 반영 후 `1`로 최종 확인되어 정상 합격하였습니다.
- 지시서 79행 특이사항 조건 발동: `[A]` 랜딩 `curl` 결과가 `0`으로 측정되었습니다.
- 원인: `src/App.tsx` (번들 `index-CwuHHv8l.js`)에는 문구가 정상 반영되어 브라우저 상에서는 정상 노출되나, 랜딩 페이지의 프리렌더 정적 블록(`index.html` 내 `<div id="root">`)에는 반영되어 있지 않아 브라우저 JS 실행 전 단순 `curl` 상에서는 0으로 감지됩니다.
- 지시서 79행 및 84행 규정에 따라 소스 코드 및 프리렌더 블록을 임의 수정하지 않고 로부장과 대표님께 있는 그대로 보고드립니다.
