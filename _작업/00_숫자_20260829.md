# 📌 숫자 — 2026-08-29 (기계로 센 것. 기억으로 고치지 말 것)

## 1200 단어장 (미끼 상품)

| 항목 | 8/27 | **8/29** |
| :--- | ---: | ---: |
| 마스터리스트 단어 | 1,200 | 1,202 |
| 그림에 붙은 단어 | 858 | **1,137** |
| 아직 안 붙은 단어 | 475 | **65** |
| 등록된 영상(편) | 157 | **195** |
| 편당 평균 단어 | 5.5 | **7.1** |

8/27 문서가 말한 "다음 한 걸음 — 475개를 157편에 붙이는 매칭"은 끝났다.
결과: **새로 만든 영상 26개(24장면), 새 챕터 2개(RES·PERSONA), 편수 38편 증가.**

남은 65개는 그림으로 보여줄 수 없다고 판정해 뺐다. 목록·이유: `_작업/스토리이관_65개.csv`

## 실제 사이트와의 격차

| | 편 | 칸 | 챕터 |
| :--- | ---: | ---: | ---: |
| 지금 사이트 (`origin/gh-pages`, 8/21) | 66 | 498 | 12 |
| 배포 대기 중 (`dist/`, 준비 완료) | **195** | **1,379** | **14** |

`origin/main` 은 8/29 푸시 완료. **사이트 반영은 `npm run deploy` 한 줄 남았다.**

## 구동사 288편 (본 상품) — 0편

| 항목 | 수 |
| :--- | ---: |
| 목표 | 288편 |
| 만든 것 | **0편** |
| 판정용 `get on` 1편 | **없음** |

`00_우선순위.md` 기준 **"구동사 79편 제작 = `get on` 판정 통과 후"**.
그 판정용 1편이 7/28부터 지금까지 안 만들어졌다. **본 상품은 착수 전이다.**

## 세는 방법

```bash
cd ~/workspace/움직이는그림사전
python3 - <<'PY'
import csv,io,json
s=io.open('public/learning/index.html',encoding='utf-8').read()
i=s.find('const chapterData = '); j=s.index('{',i); d=0
for k in range(j,len(s)):
    if s[k]=='{': d+=1
    elif s[k]=='}':
        d-=1
        if d==0: break
D=json.loads(s[j:k+1])
used={x[0] for c in D.values() for w in c['works'] for x in w['words']}
works=sum(len(c['works']) for c in D.values())
slots=sum(len(w['words']) for c in D.values() for w in c['works'])
lv={x['단어'].strip() for x in csv.DictReader(io.open('_작업/1200_레벨태그_v3.csv',encoding='utf-8-sig'))}
print('편 %d / 칸 %d / 붙음 %d / 안붙음 %d'%(works,slots,len(lv&used),len(lv-used)))
PY
```
