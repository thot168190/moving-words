# 업무인계 — 2026-08-29 (상황그림 24장 완료 · 챕터 13·14 탑재)

> 다음 세션은 이 파일부터 읽는다.

## 오늘 한 일

### 1. 상황그림 24장 제작 완료 (영상 26개)
`public/learning/ch14/` 에 ch14_01 ~ ch14_26 (mp4 + poster.jpg)
원본은 `public/learning/_상황24/` 에 뜻이름으로 보관.

프롬프트 실수 3건을 잡았다. **다음에 프롬프트 쓸 때 반드시 확인할 것.**

| 실수 | 내용 | 고친 방법 |
|---|---|---|
| 꽃 | `watercolor bloom`(수채 번짐)을 모델이 "꽃"으로 읽어 12장에 큰 꽃이 박힘 | `irregular patch of watercolor wash, an abstract shapeless colour stain` 로 교체 + Never에 flower 계열 |
| 인형 | 팔이 옷에 묻히고 손·다리 미표현 → 코케시 인형이 됨 | 팔 분리(사이에 흰 배경 띠)·팔꿈치·손가락·다리+신발 규격 신설, Never에 doll/kokeshi/figurine |
| 무대 소실 | 요소 5개 과부하로 모델이 무대·커튼을 버림 | 요소 3개로 축소, 무대를 **맨 먼저·가장 높게** 그리도록 명시 |

**글자 인쇄는 금지어로 못 막는다.** 안내판·라벨·눈금판·해시계처럼 "원래 글자가 있는 물건"은 소재 자체를 안 쓰는 게 유일한 해법.

프롬프트 원본: `/root/w/pr`(1차) `/root/w/pr2`(2차) `/root/w/pr3`(C01~C04) — 컨테이너라 세션 종료 시 사라짐. 필요하면 다시 조립.

### 2. 챕터 13·14 탑재
- **챕터 13 · RES 사물이 말하는 상황** — 29편 / 205단어
- **챕터 14 · PERSONA 사람이 말하는 상황** — 20편 / 145단어
- 앱 전체 **195편 / 1,379칸** / 1200 중 1137개 배정

백업: `public/learning/index.html.bak-ch1314-202608290424`

좌표는 포스터 26장에 10% 격자를 씌워 **한 장씩 직접 보고** 찍었다. 자동배치 아님.

### 3. 억지 65개 뺐다 — 오늘 가장 중요한 일
1차 배정 때 "415개 전량 커버 / 남음 0"을 목표로 잡아서 **그림에 없는 단어를 그림 위에 찍는** 잘못을 했다.
`tear`(눈물)를 작업대에, `gun`·`ghost`를 상자 그림에, `cigarette`을 식탁에 찍었다.
`spell`·`edit`·`paragraph`·`recipe`는 어제 4-05·3-01에서 **직접 뺐던 단어를 같은 이유로 다시 넣은** 것이다.

또한 **레벨 기준을 CEFR로 바꿔치기**했다. 대표님 3법칙 2번은 "포스터에 보이는 것 = 레벨1"인데
1200 CSV의 레벨 열(A1·A2/B1·B2)을 그대로 썼다. 그래서 `although`·`since`·`whether` 같은 접속사가 레벨1에 들어갔다.

→ 65개를 빼고 415→350, 레벨은 26장을 다시 보며 그림 기준으로 재분류했다.
→ 뺀 목록: `_작업/스토리이관_65개.csv` (단어·뜻·원래장면·뺀이유)
→ 레벨1이 0개인 편이 여럿 생겼는데 그게 정직한 결과다. 접속사 그림엔 레벨1이 없다.

**교훈: 숫자(남음 0)를 목표로 잡으면 반드시 억지가 생긴다. 그림이 판사다.**

## 남은 일 (우선순위 순)

### A. 그림 결함 — **지금 재제작할 건 0편**
어제 146편 검수 때 찾은 기존 결함이고, 문제 단어는 이미 빼서 앱은 정상 작동한다.
대표님 `00_우선순위.md` 원칙("손님이 없는데 디자인 다듬는 건 순서가 틀렸다")에 따라 **지금 하지 않는다.**

| 구분 | 편 | 처리 |
|---|---|---|
| 대체품 있음 | `1-12` `1-13` | **재제작 불필요.** `public/learning/_후보/`에 A·B 두 판씩 있음. 판정 후 **복사만** 하면 끝 |
| 채색 벗어남 | `4-06` `4-08` `4-09` `4-10` `5-17` `6-10` `7-01` `10-05` | 미관 문제. 손님 생긴 뒤 |
| 글자 인쇄 | `3-01` `4-07` `5-07` `5-16` `5-18` `5-19` `5-20` `5-24` | 대부분 구석에 작게. `5-20`(POLICE 크게)만 눈에 띔 |
| 표정 불일치 | `7-04` | 단어 7개 이미 뺐고 4칸으로 살아 있음 |

### B. 배포 — 미배포 상태
- git 미커밋 175개 파일, 마지막 커밋은 "152편/807단어" 시점. 지금 195편/1,379칸.
- 푸시는 코다리 담당. 로부장은 배포하지 않는다.
- `dist-final/index.html`은 파인더 더블클릭하면 깨진다(절대경로). 반드시 서버로 열 것.

### C. 정리
- `_to_delete` 158MB(workspace) + 46MB(Downloads) — 대표님이 직접 삭제
- `public/learning/_창고` 41개, `ch13/` 폴더 7편(기계·군용품·박물관, 미등록)
- `Downloads/veo-folder-1` 102개 mp4 — 단어장용은 전부 뽑아 옮겼음

## 규칙 (대표님이 정한 것 — 어기지 말 것)
1. **한 번에 한 편** — 전체 일괄 채움·일괄 빼기 금지
2. **그림이 판사** — 포스터에 보이는 것 = 레벨1, 장면에서 통하는 연관어 = 레벨2, 안 통하면 스토리 몫으로 이관
3. **잠금 장부** — 확정 편은 다시 안 연다
