# v5.6.3 (2026-08-20) — 다음 프롬프트 교체 복구

- 생성 클릭 1.8초 뒤 실제 Backspace 키로 제출된 프롬프트를 비움.
- Escape 키로 펼쳐진 편집창을 닫음.
- 다음 컷에서 이전 입력창과 버튼 DOM 캐시를 폐기하고 새로 탐색.
- 비우기가 실패해도 다음 입력 시 전체 선택·교체하며 재클릭은 하지 않음.

# v5.6.2 (2026-08-20) — 생성 성공 오판 수정

- Flow가 생성 후에도 입력창 내용을 유지하는 현재 동작 반영.
- 입력창이 비워졌는지를 성공 조건에서 제거.
- CDP 단일 클릭 전송이 성공하면 즉시 생성 대기 후 다음 큐로 진행.
- 단일 클릭과 30초 중복 잠금은 그대로 유지.

# v5.6.1 (2026-08-20) — 3중 생성 긴급 수정

- CDP 클릭에서 터치 이벤트를 삭제하고 마우스 클릭 한 번만 전송.
- 클릭 결과 확인이 늦어도 DOM 폴백과 재클릭을 절대 수행하지 않음.
- 동일 프롬프트의 30초 이내 중복 제출을 저장소 수준에서 차단.
- 사이드패널 상단에 `현재 버전 v5.6.1 · 단일 클릭` 표시.

# v5.6.0 (2026-08-20) — Flow 최신 편집창 복구

- 긴 프롬프트는 새 컷번호가 나올 때만 분리하고 내부 줄바꿈을 그대로 보존.
- `chrome.debugger`의 실제 입력 방식으로 React 편집창에 텍스트 입력.
- 영상 설정을 8초 · 16:9 · 1개로 고정.
- 정지 후 완료 다음 항목부터 이어서 실행 가능.
- V560 전용 메시지로 이전 확장 프로그램과 동시 실행되는 문제 차단.

# v5.5.0 (2026-07-12) — 로부장 (라이브 DOM 채취 2차)
- **Veo 동영상 벌크 1단계**: 큐 문법 `컷 | 프롬프트 @@ 애셋 @@@ 8s` (4s/6s/8s). 컷마다 생성 설정 패널을 열어 동영상 모드+초수 자동 설정. 초수 표기 없으면 이미지 모드.
- **출력 수 1x 강제**: 매 컷 설정 패널에서 1x 클릭 — 2장 생성(크레딧 2배) 원천 차단.
- 채취 셀렉터(2026-07-12): 설정 칩 = /Nano Banana|Veo/ 버튼(포인터 이벤트 시퀀스 필수, .click() 무효) / 모드 = 'image이미지'·'play_circle동영상' / 초수 = '4s' '6s' '8s' / 출력 = '1x'~'x4' / 모델 = 'Veo 3.1 - Lite' 드롭다운 / 크레딧 라벨 "생성 시 N크레딧" / 시작 프레임 = '프레임'·'애셋' 탭.
- **미검증**: 동영상 모드에서 시작 프레임을 '애셋' 탭으로 첨부하는 경로(피커 동일 여부) — 실전 1컷 테스트 필수. 실패 시 시작 프레임은 수동 첨부 + 프롬프트만 벌크.

# v5.4.1 (2026-07-11) — 로부장
- 더블 생성(2장씩) 버그 수정: 생성 클릭 시 CDP+DOM 폴백이 연달아 눌리던 것 → 클릭 후 입력창 비움 여부로 성공 검증, 미반영 시에만 1회 재클릭.

# v5.4.0 (2026-07-11) — 로부장 (라이브 DOM 채취 기반, 크롬 연결 진단)
- **원인 확정**: 페이지의 유일한 file input은 미디어 라이브러리 업로더 → 기존 파일 주입은 라이브러리로만 들어가고 컴포저 첨부가 안 됐음.
- **첨부 방식 전면 교체**: 파일 업로드 폐기 → Flow 정식 경로 자동화: 컴포저 + (add_2) → "애셋 검색" 입력 → 결과 행 클릭 → "프롬프트에 추가".
- **컷별 인그리디언트 지원**: 큐 줄 형식 `컷 | 프롬프트 @@ 애셋이름1; 애셋이름2; 애셋이름3` (이름 = Flow 라이브러리 애셋 이름, 순서 = INGREDIENT 번호).
- 컷 시작마다 기존 칩 전부 제거(cancel) 후 재첨부 — 이전 컷 잔재 방지.
- 채취 셀렉터: + 버튼 = "add_2만들기" / 생성 = "arrow_forward만들기" / 칩 제거 = "cancel" / 검색 = input[aria-label="애셋 검색"] / 추가 = "프롬프트에 추가".

# v5.3.4 (2026-07-11) — 로부장
- 인그리디언트 "시작 시 1회 업로드" 설계 폐기 → **컷마다 첨부 상태 확인·보장** (Flow가 생성 후 첨부를 초기화해도 동작)
- 파일 주입 대상을 **컴포저(프롬프트 박스) 스코프 우선**으로 변경 — 미디어 라이브러리로만 들어가던 오첨부 방지 (+200 가중치)
- 컴포저 내 + 첨부 버튼 탐색 로직 신설 (생성/에이전트 버튼 강한 페널티)
- 첨부 후 칩(img) 검증 로그 추가 — "주입됐는데 칩 없음"이면 경고로 표시 (원인 추적용)
- 주의: Flow UI 변경에 따른 추정 패치. 1컷 테스트 후 로그를 로부장에게 전달할 것.

# 철만이 Flow 벌크 버전 기록

## 5.3.3 | 2026-06-18

- 대표님 제보 반영: 프롬프트 입력은 성공했지만 로그상 `클릭 대상: "more_vert더 생성하기"`가 잡혀 실제 하단 생성 화살표가 아니라 카드/메뉴 버튼을 누르는 문제 수정.
- `more_vert`, `more_horiz`, `더 생성하기`, 옵션/overflow 계열 버튼은 생성 버튼 후보에서 완전 제외.
- 하단 프롬프트 입력창을 감싸는 composer 박스 안의 버튼을 우선 검색하고, composer 오른쪽 아래의 작은 화살표 버튼에 높은 점수를 부여.
- 버전 전용 메시지를 `V533`으로 올려 구버전 content script 잔상과 명령이 섞이지 않게 함.

## 5.3.2 | 2026-06-18

- 대표님 지적: "오토플로우인데 매번 지켜서서 인그리디언트 올리는 게 말이 되냐" 반영.
- 인그리디언트 업로드 체크박스 제거 — 패널에 이미지 있으면 시작 시 자동으로 Flow에 업로드.
- 이미지 없으면 기존처럼 Flow에 이미 세팅된 인그리디언트 그대로 사용.
- `getSettings()`에서 `uploadIngredientsOnStart`를 `ingredientImages.length > 0`으로 자동 결정.
- 버전 전용 메시지를 `V532`로 올림.

## 5.3.1 | 2026-06-18

- 대표님 제보 반영: 컷당 2장씩 생성되는 문제 수정.
- 원인: 생성 버튼 클릭 후 600ms 뒤에도 버튼이 활성화 상태면 Enter 폴백이 추가로 발동되어 2번 생성됨.
- 수정: Enter 키 폴백 완전 제거. 5.3.0부터 올바른 버튼(arrow_forward)을 찾게 됐으므로 폴백 불필요.
- 버전 전용 메시지를 `V531`로 올림.

## 5.3.0 | 2026-06-18

- 대표님 제보 반영: 로그에서 `클릭 대상: "imageFeast courtyard with people eating"` 확인 — 실제 생성 버튼이 아니라 Flow 입력창 아래에 표시되는 프롬프트 예시 suggestion 칩을 생성 버튼으로 잘못 인식하는 문제 수정.
- `isBadGenerateButton`: 텍스트 25자 초과 버튼은 생성 버튼 후보에서 제외.
- `scorePromptSendButton`: 텍스트 없는 아이콘 버튼에 +60점 보너스, 텍스트 10자 초과 시 -80점 페널티 추가.
- 실제 생성 버튼(텍스트 없는 작은 화살표 아이콘 버튼)이 최우선으로 선택되도록 개선.
- 버전 전용 메시지를 `V530`으로 올려 구버전 content script 잔상과 명령이 섞이지 않게 함.

## 5.2.9 | 2026-06-18

- 대표님 제보 재반영: 5.2.8 수정으로도 변화 없음 확인.
- 근본 원인 재분석: Flow 프롬프트창이 contenteditable인데, `execCommand('insertText')`는 DOM에만 텍스트를 넣고 React 내부 상태는 안 바꿔서 생성 버튼이 계속 disabled 상태로 남음.
- 수정: contenteditable 입력 방식을 CDP `Input.insertText`로 교체 — 실제 키보드 입력처럼 처리되어 React가 상태를 정상 업데이트하고 생성 버튼이 활성화됨.
- `background.js`에 `CDP_INSERT_TEXT` 핸들러 추가 (`Input.insertText` CDP 명령 사용).
- CDP 실패 시 기존 `execCommand` 폴백 유지.
- 버전 전용 메시지를 `V529`로 올려 구버전 content script 잔상과 명령이 섞이지 않게 함.

## 5.2.8 | 2026-06-18

- 대표님 제보 반영: 프롬프트는 입력되는데 실제 생성이 안 되는 문제 수정.
- CDP 클릭에 `pointerType: 'mouse'` 추가 — Google Flow의 React 버튼이 pointer 타입을 체크하는 경우 대응.
- CDP 클릭에 touchStart/touchEnd 시도 추가 (실패해도 무시하는 폴백).
- `setInputValue` 후 400ms 대기 추가 — React 상태 업데이트 완료 후 버튼 활성화 보장.
- 버튼 클릭 후 600ms 경과 시 버튼이 여전히 활성화 상태면(= 클릭 미반응) 입력창에 Enter 키 이벤트 폴백 전송.
- 버전 전용 메시지를 `V528`로 올려 구버전 content script 잔상과 명령이 섞이지 않게 함.

## 5.2.7 | 2026-06-18

- 대표님 제보 반영: 시작할 때마다 패널 인그리디언트가 Flow 작업창에 다시 주입되어 같은 이미지가 중복 생성되는 문제 수정.
- 패널 이미지는 기본적으로 번호/순서 확인용으로 유지하고, Flow 작업창 재업로드는 하지 않음.
- 새 체크박스 `시작할 때 Flow 작업창에 이미지 다시 업로드`를 추가. 이 체크를 켠 경우에만 Flow에 이미지를 다시 주입.
- 실행 로그에 `인그리디언트 이미지 n개 확인용 유지, Flow 작업창 재업로드 생략`을 표시.
- 버전 전용 메시지를 `V527`로 올려 구버전 content script 잔상과 명령이 섞이지 않게 함.

## 5.2.6 | 2026-06-18

- 대표님 제보 반영: 프롬프트가 Flow 하단 생성 입력창이 아니라 상단 검색창에 입력되어, 화면이 검색 결과 없음 상태로 바뀌고 실제 생성은 진행되지 않는 문제 수정.
- 검색/필터/상단 넓은 입력창은 프롬프트 입력 후보에서 차단.
- `무엇을 만들고 싶으신가요?`, `에이전트`, `Nano Banana`, `생성 시`, `인그리디언트` 문맥이 있는 하단 composer 입력창을 우선 감지.
- 실행 로그에 `입력창 확인`을 추가하고, 진단 로그에는 입력 후보별 점수와 차단 여부를 표시해 다음 UI 변경 때 원인을 바로 볼 수 있게 함.
- 버전 전용 메시지를 `V526`으로 올려 구버전 content script 잔상과 명령이 섞이지 않게 함.

## 5.2.5 | 2026-06-18

- 대표님 제보 반영: 로그상 `클릭 대상: "search검색"` 또는 `arrow_back검색 닫기`가 잡히며 실제 생성이 되지 않는 문제 수정.
- 검색/필터/닫기/뒤로가기/설정/도움말/라이브러리 계열 버튼은 생성 버튼 후보에서 제외.
- 프롬프트 입력창 오른쪽 아래에 있는 작은 화살표 버튼을 위치 기반으로 우선 감지.
- 버전 전용 메시지를 `V525`로 올려 구버전 content script 잔상과 명령이 섞이지 않게 함.

## 5.2.4 | 2026-06-18

- 대표님 제보 반영: 인그리디언트만 업로드되고 프롬프트 진행이 멈추는 문제 대응.
- 원인 1: `컷마다 인그리디언트를 바꿀 때만 멈춤` 옵션이 켜진 상태로 남아 있으면, 패널에서 인그리디언트를 올렸는데도 첫 컷에서 "계속" 대기 상태가 됨.
- 수정 1: 패널에 인그리디언트가 1개 이상 있으면 해당 옵션을 자동 해제/비활성화하고, 실행 설정에서도 강제로 `false` 처리.
- 원인 2: Flow 탭에 구버전 content script가 남아 있을 때 새 스크립트 주입과 명령 처리가 꼬일 수 있음.
- 수정 2: guard 플래그를 `__cheolmaniFlowBulkV524`로 변경하고, 시작/정지/계속/진단/PING 메시지를 `V524` 전용 타입으로 변경.

## 5.2.3 | 2026-06-18

- 대표님 요청 반영: 업로드된 인그리디언트가 글씨로만 보이지 않고 실제 이미지로 보이도록 썸네일 카드 UI 추가.
- 선택 현황 아래에 최대 3개 썸네일을 그리드로 표시.
- 각 썸네일에 번호, 파일명, 용량을 함께 표시해 철만이/마당/소품 구분 가능.
- content script 버전 체크를 `5.2.3`으로 올려 Flow 탭 구버전 잔상 방지.

## 5.2.2 | 2026-06-18

- 대표님 제보 반영: 철만이 이미지를 넣고 마당 이미지를 추가하면 기존 철만이가 빠지고 마당 1개만 남는 문제 수정.
- 원인: 새 이미지 입력 때 기존 `ingredientImages` 배열에 추가하지 않고 새 목록으로 교체하고 있었다.
- 수정: 붙여넣기/드롭/보조 선택 모두 기존 목록 뒤에 누적 추가하고, 최대 3개까지만 유지.
- 3개가 이미 찬 경우에는 "이미 3/3개가 선택되어 있습니다" 경고를 표시하고 변경하지 않음.
- content script 버전 체크를 `5.2.2`로 올려 Flow 탭 구버전 잔상 방지.

## 5.2.1 | 2026-06-18

- 대표님 요청 반영: 인그리디언트 최대 3개 가능 표시를 UI에 추가.
- 붙여넣기/드롭/보조 선택에서 4개 이상 들어오면 앞의 3개만 사용하고 경고 로그 표시.
- 선택 현황을 `n/3개 선택됨`으로 표시.
- content script 버전 체크를 `5.2.1`로 올려 Flow 탭 구버전 잔상 방지.

## 5.2.0 | 2026-06-18

- 대표님 지적 반영: "저건 파일로 만드는거잖아" 문제 수정.
- 기본 UI에서 CSV/파일 불러오기 중심 흐름을 제거하고, 인그리디언트 박스에 이미지 붙여넣기 또는 드롭하는 방식으로 변경.
- 프롬프트는 별도 파일 없이 textarea에 엔터 줄바꿈으로만 구분.
- 파일 선택 버튼은 붙여넣기/드롭이 안 될 때 쓰는 보조 수단으로 격하.
- content script 버전 체크를 `5.2.0`으로 올려 Flow 탭 구버전 잔상 방지.

## 5.1.0 | 2026-06-18

- 대표님 요청 반영: 벌크 패널 안에서 인그리디언트 이미지를 업로드하고 프롬프트는 줄바꿈만으로 구분하는 방식 추가.
- 시작 시 이미지 파일을 Flow 업로드 input에 한 번 주입한 뒤, 같은 인그리디언트로 여러 프롬프트를 순차 생성.
- 이미지 미선택 시 기존 v5.0.3처럼 Flow에 이미 세팅된 인그리디언트를 유지.
- 대용량 이미지 저장을 위해 `unlimitedStorage` 권한 추가.
- 비용 방지 안내: 12화 전체 실행 전 2~3컷 드라이런/소량 테스트 권장.

## 5.0.3 | 2026-06-18

- 크롬 확장 폴더 선택창에서 `flow-bulk-extension`과 구버전 백업 폴더가 헷갈리는 문제 대응.
- 별도 선택용 폴더 `cheolmani-flow-bulk-v503` 생성.
- 확장 이름을 `철만이 Flow 벌크 v5.0.3`으로 변경해 카드에서 바로 식별 가능하게 함.

## 5.0.2 | 2026-06-18

- 크롬 확장 프로그램 화면에 `Flow 대량 생성기 4.1.1`이 계속 표시되는 문제 확인.
- 원인: 새 작업 폴더가 아니라 `flow-bulk-extension_legacy_20260618_143622` 백업 폴더가 크롬에 로드된 상태.
- 새 작업 폴더를 `5.0.2`로 올려 구버전과 더 확실히 구분.

## 5.0.1 | 2026-06-18

- 대표님 지시 반영: 수정 후 버전 숫자를 반드시 올려 기록.
- 크롬 확장 화면에서 구분되도록 `manifest.json` 버전을 `5.0.1`로 변경.
- 사이드패널 표시 버전을 `v5.0.1`로 변경.
- README에 버전 기록 섹션 추가.

## 5.0.0 | 2026-06-18

- 기존 누적 실험판을 백업하고 새 구조로 재작성.
- 기본 목적을 `같은 인그리디언트 세팅 + 프롬프트 여러 줄 연속 생성`으로 단순화.
- API 캡처, 자동 다운로드, 하드코딩 배치, 복잡한 인그리디언트 자동 검색 제거.
